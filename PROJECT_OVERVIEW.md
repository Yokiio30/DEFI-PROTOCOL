# 项目说明 — DeFi Protocol Suite

> SC6107 · Option 3: Upgradeable DeFi Protocol Suite · Group 5

---

## 这个项目是做什么的

这是一个运行在以太坊区块链上的**去中心化金融（DeFi）协议**，由三个模块组成：

1. **借贷池**：存钱赚利息，或者抵押资产借钱
2. **流动性挖矿**：把代币质押进来，系统自动发奖励
3. **治理**：持有治理代币的人可以发起投票，决定协议怎么改

三个模块都可以升级——但不是随便谁都能升级，必须通过社区投票，而且投票通过之后还要等两天才能执行。没有任何"管理员私钥"可以绕过这个流程。

---

## 用了哪些技术

### 智能合约（后端）

| 技术 | 用途 |
|------|------|
| **Solidity 0.8.22** | 智能合约编程语言 |
| **Foundry**（forge / anvil / cast） | 合约编译、测试、部署工具 |
| **OpenZeppelin 5.x** | 经过审计的合约库（ERC-20、权限控制、升级代理等） |
| **UUPS 代理模式（EIP-1967）** | 让合约逻辑可升级，同时保留所有用户数据 |

### 前端（用户界面）

| 技术 | 用途 |
|------|------|
| **React 18** | 构建页面组件 |
| **ethers.js v6** | 在浏览器里调用智能合约 |
| **Vite** | 前端构建工具 |
| **MetaMask** | 用户钱包，用于签名交易 |

---

## 三个模块的功能

### 模块一：借贷池（LendingPool）

**大白话：** 就像银行，你存钱进去赚利息，别人也可以抵押资产向池子借钱。

**能做什么：**

- **存款**：把 WETH 或 USDC 存进去，系统记下你的份额，利息自动累积
- **取款**：拿走本金加上赚到的利息
- **借款**：先存钱作为抵押，最多可以借抵押价值的 75%
- **还款**：把借的钱还回去，恢复健康状态
- **清算**：如果某人的抵押比例太低（健康系数 < 1），任何人都可以来清算，替他还一部分债，同时拿走他的抵押物加 5% 的奖励

**利率怎么算：**

利率会随借款量变化——借的人越多，利率越高，这样就会刺激有人来存款、有人去还款，维持池子里有足够的流动性。

```
借款少（利用率 < 80%）：利率缓慢上升，从 2% 涨到 10%
借款多（利用率 > 80%）：利率急剧上升，最高可达 260%
```

**健康系数：**
```
健康系数 = 抵押品价值 × 清算门槛 / 债务总额

∞    → 没有借款，最安全
≥ 1.5 → 安全（绿色）
1.0 ~ 1.5 → 需要留意（黄色）
< 1.0 → 可以被清算（红色）
```

---

### 模块二：流动性挖矿（LiquidityMining）

**大白话：** 把代币质押进来"挖矿"，系统每秒钟自动给你发 DPT（治理代币）奖励。

**能做什么：**

- **质押**：把 WETH 或 USDC 存进挖矿合约，开始赚奖励
- **收割（Harvest）**：随时领取已经累积的 DPT 奖励，不用动质押的本金
- **取消质押**：拿回质押的代币，同时自动领走所有未领的奖励
- **紧急提款**：极端情况下可以放弃奖励，立即拿回本金

**奖励怎么分：**

系统设置了两个矿池，奖励按权重分：
- Pool #0（WETH 池）：权重 100
- Pool #1（USDC 池）：权重 50
- 总权重 150，所以 WETH 池拿到总奖励的 2/3，USDC 池拿 1/3

同一个池里，谁质押得多，谁分到的奖励就多，按比例计算，实时累积。

**奖励计算原理（Masterchef 算法）：**
每秒只更新一个全局的"每份额累积奖励"数字，每个用户领奖励时拿这个数减去自己的"已领记录"，O(1) 时间完成，不管池子里有多少人都一样快。

---

### 模块三：治理（Governance）

**大白话：** 持有 DPT 代币就有投票权，可以对协议的修改发起提案，大家投票决定。

**能做什么：**

- **提交提案**：需要持有至少 1,000 DPT，写清楚要改什么、改哪个合约
- **投票**：提案进入投票期（3 天）后，选择赞成 / 反对 / 弃权，你持有多少 DPT 就代表多少票
- **进入队列**：投票通过（赞成票 > 10,000 DPT，且赞成 > 反对）后，点 Queue 进入等待期
- **执行**：等满 2 天的 Timelock 之后，才能真正执行提案里的操作
- **取消**：提案发起人或守护者（Guardian）可以在执行前取消

**提案完整流程：**
```
提交提案 → 等待 1 小时（Voting Delay）
         → 开放投票 3 天（Voting Period）
         → 投票通过 → 进入队列
         → 等 2 天（Timelock）
         → 执行提案内容
```

**为什么要等那么久：**
Timelock 期间任何人都可以看到即将执行的操作，如果发现是恶意升级，Guardian（紧急多签钱包）可以在这段时间内取消它，保护用户资金。

---

## 三个模块之间怎么联动

```
┌──────────────────────────────────────────────────────────────┐
│                      用户                                     │
│  存款 / 借款 / 还款     质押 / 收割       发提案 / 投票        │
└────────┬──────────────────┬───────────────────┬──────────────┘
         │                  │                   │
         ▼                  ▼                   ▼
   ┌──────────┐      ┌────────────┐      ┌──────────┐
   │ 借贷池   │      │ 流动性挖矿 │─mint→│ 治理代币 │
   │LendingPool│     │LiquidityMining│    │  DPT     │
   └──────────┘      └────────────┘      └────┬─────┘
         ▲                  ▲                  │
         │                  │         持有 DPT = 投票权
         └──────────────────┴──────────────────┘
                    治理合约（Governance）
                  可以通过投票升级这三个合约
```

**关键的联动关系：**

1. **流动性挖矿 → 治理代币（DPT）**
   质押 WETH/USDC 到挖矿合约，赚到的奖励是 DPT 代币，DPT 会直接转入你的钱包。

2. **DPT → 治理权力**
   你钱包里的 DPT 余额直接决定你的投票权重。想提提案需要至少 1,000 DPT，提案通过需要总赞成票超过 10,000 DPT。

3. **治理 → 升级三个合约**
   治理合约是整个系统的"最高权力机关"。如果要给借贷池添加新资产、调整挖矿奖励率、修改利率参数，都必须走治理投票，投票通过 + 等待 Timelock 才能执行，没有任何人可以绕过。

4. **经济闭环**
   存款赚利息 → 资金效率提升 → 质押获得 DPT → 获得治理权 → 可以投票改善协议参数 → 协议更好 → 吸引更多存款

---

## 代币（DPT）说明

| 参数 | 值 |
|------|-----|
| 名称 | DeFi Protocol Token |
| 符号 | DPT |
| 总供应上限 | 1 亿枚（100,000,000 DPT） |
| 初始发行 | 部署时 1,000 万枚给部署者 |
| 剩余 9,000 万 | 通过流动性挖矿慢慢释放 |
| 用途 | 流动性挖矿奖励 + 治理投票权 |

---

## 可升级性说明

三个核心合约都用了 **UUPS 代理模式**：合约分成"代理合约"和"逻辑合约"两层。用户的资金和数据保存在代理合约里，逻辑合约只负责处理业务逻辑。

升级时只换逻辑合约，代理合约（和里面的用户数据）完全不动。而且，谁能触发升级？只有治理合约，也就是说必须经过社区投票。

---

## 测试情况

100 个测试用例，全部通过，0 失败：

| 合约 | 测试数量 | 通过率 | 代码行覆盖率 |
|------|---------|--------|------------|
| LendingPool.sol | 34 | 100% | 94.34% |
| Governance.sol | 31 | 100% | 91.30% |
| LiquidityMining.sol | 35 | 100% | 91.60% |
| GovernanceToken.sol | — | — | 100% |

测试类型包括：单元测试、集成测试、模糊测试（随机输入）、不变量测试（大量随机操作验证系统属性）、升级测试（验证升级后用户数据不丢失）。

---

*SC6107 DeFi Protocol Suite · Group 5*
